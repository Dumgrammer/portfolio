# Keep attributes minimal — do NOT use Git LFS for portfolio assets.
# GitHub Pages serves LFS pointer files as-is, which breaks images.
# The public/ folder is small enough (~8MB) for normal Git blobs.
